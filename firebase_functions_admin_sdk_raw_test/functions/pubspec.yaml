name: my_function_raw
description: An app using Dart with Cloud Functions for Firebase
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  firebase_functions: ">=0.6.0"

dev_dependencies:
  build_runner: ">=2.4.0"
  lints: ">=6.0.0"
resolution: workspace
